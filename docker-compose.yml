name: pulse

services:
  bot:
    build: .
    container_name: pulse
    restart: unless-stopped
    init: true
    stop_grace_period: 15s
    env_file:
      - .env.docker
    volumes:
      - .:/app
      - /app/node_modules
